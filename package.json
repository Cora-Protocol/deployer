{
  "name": "hardhat-project",
  "scripts": {
    "compile": "hardhat compile",
    "test": "FORKING=true hardhat test",
    "deploy:arbitrum-testnet": "hardhat deploy --network arbitrum_testnet",
    "deploy:arbitrum": "hardhat deploy --network arbitrum",
    "deploy:arbitrum-testnet:script": "hardhat run scripts/deploy.ts --network arbitrum_testnet",
    "deploy:arbitrum:script": "hardhat run scripts/deploy.ts --network arbitrum"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.16.1",
    "hardhat-deploy": "^0.11.34"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "web3-utils": "^4.0.2"
  }
}